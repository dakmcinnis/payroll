<div id="<%= dom_id timesheet %>">
  <p>
    <strong>User:</strong>
    <%= User.find(timesheet.user_id).display_full_name %>
  </p>

  <p>
    <strong>Pay period:</strong>
    <%= PayPeriod.find(timesheet.pay_period_id).display_dates %>
  </p>
</div>
